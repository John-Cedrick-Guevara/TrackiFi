{
  "name": "my-app",
  "type": "module",
  "scripts": {
    "dev": "cd server && wrangler dev --local",
    "dev:remote": "cd server && wrangler dev",
    "deploy": "cd server && wrangler deploy --minify",
    "cf-typegen": "cd server && wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "@supabase/supabase-js": "^2.93.2",
    "hono": "^4.11.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "supabase": "^2.72.9",
    "wrangler": "^4.4.0"
  }
}